# -*- coding: utf-8 -*-
"""Malaxis Fleet - modular node agent package (v1.1.0).

The agent is shipped as this package (served by the master as agent_src.zip)
plus a thin node_agent.py bootstrap launcher that keeps the legacy
`import node_agent` CLI contract working inside the container.
"""

__version__ = "1.1.0"
